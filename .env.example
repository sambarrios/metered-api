# Copy to .env and fill in real values. Never commit .env.
# Used by docker-compose for variable substitution.

POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=

# HMAC secret for payment webhook verification (must match your payment processor)
WEBHOOK_SIGNING_SECRET=

# HS256 secret for staff/ops JWT auth
STAFF_JWT_SECRET=
